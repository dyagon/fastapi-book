# Redis Configuration
# Copy this file to .env and modify as needed

# Environment (development, production, testing, local)
ENVIRONMENT=development

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=26379
REDIS_PASSWORD=redis_password
REDIS_DB=0
REDIS_MAX_CONNECTIONS=20

# Alternative: Direct Redis URL (overrides individual settings)
# REDIS_URL=redis://:redis_password@localhost:26379/0

# Database Configuration (if needed)
# DATABASE_URL=postgresql+asyncpg://admin:admin123@localhost:25432/fastapi_book
